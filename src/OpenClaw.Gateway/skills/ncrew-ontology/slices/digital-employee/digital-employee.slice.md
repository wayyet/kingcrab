# 数字员工 ontology slice

## 主题

- 任务名称：数字员工本体切片初始化
- 切片主题：数字员工
- 任务目标：抽取数字员工子域的最小概念、关系与约束，作为后续 projection 和 consumer contract 的上游语义输入。

## 当前范围

- 纳入：数字员工主体、能力配置、任务执行边界。
- 排除：具体渠道接入实现、厂商 SDK 细节、运行时审计日志结构。

## 当前来源

- 待补充与数字员工主题直接相关的业务说明、代码定义、配置约束和术语文档。
- 当前骨架先提供一个可继续扩展的 producer 入口，不把未验证经验直接写成本体事实。

## 初始概念建议

- 数字员工：被系统识别和调度的业务执行主体。
- 能力配置：定义数字员工允许使用的能力、工具和上下文边界。
- 任务单元：数字员工在一次业务流程中被分配的可执行任务。

## 后续建议

1. 补齐权威来源，优先使用产品说明、配置结构和代码中的稳定类型定义。
2. 明确“数字员工”与“agent”“workflow”“skill”之间的边界和术语映射。
3. 在 slice 稳定后，再决定要投影成 `domain-model`、`json-schema`、`prompt-constraint` 还是 `workflow-contract`。